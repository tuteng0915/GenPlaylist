_target_: lightning.pytorch.strategies.DDPStrategy
# CFG-only parameters (for example context_map) are intentionally inactive when
# sampling.cfg_enabled=false, so DDP must tolerate them during backbone SFT.
find_unused_parameters: true
